Port the movement editing logic from ENGINE/dev_mode/frame_editor_session.cpp into ENGINE/dev_mode/frame_editors/MovementFrameEditor.hpp/.cpp (selection, snapping, smoothing/curve controls, per-frame offsets, apply-all buttons, render overlays/world). Ensure persistence uses ManifestTransaction only (no direct file writes), and reuse the same builder/commit logic currently used by the legacy session so JSON output is byte-identical for movement edits.
Move attack-geometry editing (node selection, axis adjust, rotation handle, render overlays, commit logic) out of FrameEditorSession into ENGINE/dev_mode/frame_editors/AttackGeoFrameEditor.hpp/.cpp. Use SelectionState/AxisAdjuster, and commit edits via ManifestTransaction with the same payload structure the legacy editor produced.
Port hit-geometry editing and rendering from ENGINE/dev_mode/frame_editor_session.cpp into ENGINE/dev_mode/frame_editors/HitGeoFrameEditor.hpp/.cpp. Ensure selection/axis adjustment uses the shared helpers and manifest commits remain byte-for-byte compatible with the legacy output.