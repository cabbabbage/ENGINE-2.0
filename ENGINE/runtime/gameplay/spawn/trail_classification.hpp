#pragma once

#include <string>

namespace dynamic_spawn {

// Only allowed trail-rule entry point for dynamic spawn trail classification.
bool is_trail_area_label(const std::string& value);

} // namespace dynamic_spawn
